# Local development credentials for Bedrock via AWS SDK
AWS_REGION=ap-northeast-1
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0
BEDROCK_MAX_OUTPUT_TOKENS=1024

# Provide short-lived AWS credentials with permission to call Bedrock Runtime.
# Never commit actual secrets; populate only in your untracked .env file.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
